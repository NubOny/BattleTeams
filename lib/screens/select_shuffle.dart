import 'package:flutter/material.dart';

class SelectShuffle extends StatelessWidget {
  const SelectShuffle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Página em Branco')));
  }
}
