import 'package:flutter/material.dart';

class SessionsPages extends StatelessWidget {
  const SessionsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Página em Branco')));
  }
}
